/*
 * FEVER Retrieval
 *
 * The multimodal media database. Images, video, audio, office documents, and LiDAR in one table: hybrid vector + full-text search, dedupe, seek-to-moment video hits, whisper transcript spans, collection grouping, and asset-to-asset attachments. Tenancy, scale, and isolation are automatic; the tenant is taken from the API key.
 *
 * The version of the OpenAPI document: 0.4.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// MediaSpan : The transcript span that matched: where in the audio/video the hit is. Includes the literal segment text so the UI can highlight it.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MediaSpan {
    #[serde(rename = "end_ms")]
    pub end_ms: i32,
    #[serde(rename = "start_ms")]
    pub start_ms: i32,
    #[serde(rename = "text", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub text: Option<Option<String>>,
}

impl MediaSpan {
    /// The transcript span that matched: where in the audio/video the hit is. Includes the literal segment text so the UI can highlight it.
    pub fn new(end_ms: i32, start_ms: i32) -> MediaSpan {
        MediaSpan {
            end_ms,
            start_ms,
            text: None,
        }
    }
}

